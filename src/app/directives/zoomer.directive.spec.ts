import { ZoomerDirective } from './zoomer.directive';

describe('ZoomerDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomerDirective();
    expect(directive).toBeTruthy();
  });
});
